<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="BirthdaysDateSelectorFragmentTextAppearance">
        <item name="textBold">true</item>
        <item name="textFontFamily">@font/nunito</item>
        <item name="textColor">@color/textColorDarker</item>
        <item name="textPadding">@dimen/spacing_tiny</item>
        <item name="textSize">@dimen/font_small</item>
    </style>

</resources>
